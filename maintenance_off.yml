---
- name: Página de mantenimiento UP
  hosts: localhost
  connection: local
  become: false
  gather_facts: false

  tasks:
    - name: Obtener pool members
      debug:
        msg: '{{ f5_credentials | default(0) }}'
